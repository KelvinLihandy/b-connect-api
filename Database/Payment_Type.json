{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [
            "payment_type_id",
            "payment_type_name"
        ],
        "properties": {
            "payment_type_id": {
                "bsonType": "string",
                "maxLength": 20,
                "description": "Primary identifier for the payment type, must be a string with max length 20"
            },
            "payment_type_name": {
                "bsonType": "string",
                "maxLength": 100,
                "description": "Name of the payment type, max length 100"
            }
        }
    }
}
